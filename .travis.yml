dist: trusty
sudo: "required"

language: "go"

go:
    - 1.8.x

services:
    - "docker"

script:
    - make build

install:
  - . $HOME/.nvm/nvm.sh
  - nvm install stable
  - nvm use stable
  - npm install
